import { QuotePageDirective } from './quote-page.directive';

describe('QuotePageDirective', () => {
  it('should create an instance', () => {
    const directive = new QuotePageDirective();
    expect(directive).toBeTruthy();
  });
});
